# The field itself. Regenerated by every absorption pass and specific to this
# machine's fleet — committing it would version a snapshot of a thing whose
# entire point is being current.
field.json
view.json
BRIEF.txt
watermarks.json

# Which repos this install watches. Local by definition; collect.py writes a
# default on first run.
fleet.json

__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
shapes.json

# Local to this machine: where to look, and what to call things.
roots.json
aliases.json

# A run log of a private fleet, not documentation of the tool.
results.md
cachesizes.json

# Receipts from rpwrap, and the advisory snapshot. Local evidence, not source.
runs/
advisories.json
